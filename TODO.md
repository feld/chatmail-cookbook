# TODO

* Get FreeBSD package repo deployed to more places, use SRV records
* Investigate supporting more OSes (standardize on s6 service mgmt for non-Linux?)
* Support more arches than amd64. Lego and Filtermail should support aarch64 now. Dovecot packages will be built for aarch64 upstream, but my FreeBSD packages are only amd64 right now.
