# THIS FILE IS AUTOGENERATED BY CHEF - DO NOT MODIFY

smtp_client_ports="{143, 465, 587, 993}"
web_ports="{80, 443}"
icmp_types="{0, 3, 8, 11}"
icmp6_types = "{ 1, 2, 3, 4, 128, 129, 135, 136 }"
ext_if="<%= @default_route_iface %>"

block return log all

set skip on lo0

pass in quick inet proto icmp icmp-type $icmp_types
pass in quick inet6 proto icmp6 icmp6-type $icmp6_types

# SSH
pass in quick proto tcp from any to any port 25 flags S/SA keep state

# SMTP submission... hard to rate limit without losing mails
pass in quick proto tcp from any to any port 25 flags S/SA keep state

# SMTP clients could be limited a bit
pass in quick inet proto tcp from any to any port $smtp_client_ports flags S/SA keep state
#    (max-src-conn 10, max-src-conn-rate 50/60, overload <bruteforce> flush global)

# Web ports
pass in quick inet proto tcp from any to any port $web_ports flags S/SA keep state
#    (max-src-conn 10, max-src-conn-rate 10/60, overload <bruteforce> flush global)

pass out proto tcp from any to any keep state
pass out proto {udp, icmp} from any to any
